<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CompotClicker"
             x:Class="CompotClicker.GamePage"
             Title="Kompot">

    <StackLayout Padding="30" Spacing="20">
        <Label Text="{Binding TotalPoints, StringFormat='Punkty: {0:F0}'}"
               FontSize="24"
               HorizontalOptions="Center" />

        <Label Text="{Binding PointsPerClick, StringFormat='Na klik: {0}'}"
               FontSize="18"
               HorizontalOptions="Center" />

        <Button ImageSource="https://png.pngtree.com/png-vector/20231017/ourmid/pngtree-fresh-apple-fruit-red-png-image_10203073.png"
                FontSize="Large"
                Background="#00000000"
                Command="{Binding ClickCommand}"></Button>

    </StackLayout>
</ContentPage>
